import styled, { css } from "styled-components";
import { Box } from '@/shared/components'; 

export const RoomChatContainer = styled(Box)`
    width: 80%;
    display: flex;
    flex-direction: column;
    position: relative;
`